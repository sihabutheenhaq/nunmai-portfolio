// Imran Khan's founder photo, embedded as a base64 data URI.
// The original public/team/imran-khan.jpg file was committed to this repo as base64 TEXT
// (not decoded binary), so browsers could never render it as an image. Embedding the same
// bytes here as a data URI is a reliable workaround that always renders correctly.
export const imranKhanPhoto = "data:image/jpeg;base64,PLACEHOLDER";
