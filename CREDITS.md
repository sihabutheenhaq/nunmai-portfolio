# Media & component credits

- Hero light beam: original real-time WebGL shader in `components/HeroBeam.tsx` (no third-party
  media or code). Huly.io was used only as a mood reference.
- Nunmai mark: `components/NunmaiMark.tsx` and `app/[lang]/icon.svg`.
- Explainer video: copy `nunmai-engine-explainer.mp4` and `nunmai-engine-explainer-poster.jpg`
  from nunmai.in/video/ into `public/video/` (see `explainer` in `lib/dictionaries/en.ts`).
- Founder photo: `public/team/imran-khan.jpg`, supplied by Nunmai.
- World map dots (`public/geo/land-dots.json`): generated from Natural Earth land data via
  `world-atlas` (public domain).
- UI components (MIT), installed with the shadcn CLI into `components/ui/`:
  - Pixel canvas: Componentry (componentry.dev), used in the footer.
  - Animated list and border beam: Magic UI (magicui.design), used in the product cards.
